---
	const {date} = Astro.props;

	const formattedDate = new Intl.DateTimeFormat(import.meta.env.PUBLIC_LC, {
    year: 'numeric',
    month: 'numeric',
    day: 'numeric' 
  }).format(Date.parse(date))
---
<time datetime={date}>{formattedDate}</time>
