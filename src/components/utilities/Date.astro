---
	const {date} = Astro.props;

  const lc = import.meta.env.PUBLIC_LC as string ?? 'en-US';
	const formattedDate = new Intl.DateTimeFormat(lc, {
    year: 'numeric',
    month: 'numeric',
    day: 'numeric' 
  }).format(Date.parse(date))
---
<time datetime={date}>{formattedDate}</time>
